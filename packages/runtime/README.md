# @agent-os/runtime

Execution runtime type contracts. Lifecycle hooks, runtime contexts, and event
shapes only — implementations land in Phase 2.

## Exports

- `RuntimeId` — branded identifier type.
- `LifecyclePhase`, `LifecycleEvent` — runtime state machine.
- `RuntimeContext`, `RuntimePort` — runtime surface area.

## Status

Phase 1.1 skeleton.
