/**
 * @agent-os/benchmarks
 *
 * Performance benchmarks and stress tests for Agent OS.
 *
 * Run benchmarks: pnpm bench
 * Run stress tests: pnpm test
 */

export const PACKAGE_NAME = '@agent-os/benchmarks' as const;
export const PACKAGE_VERSION = '0.1.0' as const;
